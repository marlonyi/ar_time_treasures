import 'package:flutter/material.dart';

class AppProvider with ChangeNotifier {
  // Aquí puedes agregar estado global, como usuario actual, lista de cápsulas, etc.
  // Por ejemplo:
  // User? currentUser;
  // List<Capsule> nearbyCapsules = [];

  // Métodos para actualizar estado
  // void setCurrentUser(User user) {
  //   currentUser = user;
  //   notifyListeners();
  // }
}
